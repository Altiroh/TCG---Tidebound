/**
 * Placeholder pour les types générés par Supabase
 * (`supabase gen types typescript`). À remplacer une fois le schéma de
 * base de données défini, pour retrouver le typage fort sur les requêtes.
 *
 * Exemple de commande, une fois le projet Supabase créé :
 *   npx supabase gen types typescript --project-id <id> > lib/supabase/types.ts
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Database {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
}
