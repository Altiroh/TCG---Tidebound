/** Le deck témoin seul, pour le confronter à un rayon. */
import { NEUTRE } from "@/scripts/preconLab/libraries/cardRates";
import type { DeckList } from "@/game/cards/decks/types";

export const LIBRARY: DeckList[] = [NEUTRE];
