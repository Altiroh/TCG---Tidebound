# Animation — changement d'Eaux

Déclenchée quand `state.environment.currentWaterId` change (`WATER_CHANGED`
dans `game/events/types.ts`, résolu par `game/actions/endTurn.ts` ou l'effet
`changeWater`).
